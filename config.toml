enable-magisk-update = false # set this to false if you do not want to receive updates for the module in magisk app
parallel-jobs = 1

# add 'enabled = false' for not patching a specific app or remove it from the config
# see https://github.com/j-hc/revanced-magisk-module/blob/main/CONFIG.md for more detailed explanations

# you can use rvmm-config-gen to generate a config

# Global Settings

patches-version = "dev"
cli-version = "dev"

# App Specific Settings

[YouTube-Extended]
app-name = "YouTube"
enabled = true
#patches-source = "inotia00/revanced-patches"
patches-source = "anddea/revanced-patches"
cli-source = "inotia00/revanced-cli"
rv-brand = "ReVanced Extended"
build-mode = "module"
excluded-patches = "'Theme' 'Enable debug logging' 'Alternative thumbnails'"
included-patches = "'MaterialYou' 'Return YouTube Username' 'Custom branding icon for YouTube'"
patcher-args = "-OappIcon=revancify_red"
apkmirror-dlurl = "https://www.apkmirror.com/apk/google-inc/youtube"
version = "auto"

[Spotify]
uptodown-dlurl = "https://spotify.en.uptodown.com/android"
version = "auto"

# END
